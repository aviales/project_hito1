<%# <#% @tweets.each do |twe|%>
<div class="mt-5">
  <table class="table">
    <thead>
    <tr>
    <th>Content</th>
    <th>Likes</th>
    <th>Retweets</th>
    <th>User</th>
    <!-- <th>Retweets</th> -->
    <th colspan="3"></th>
    </tr>
    </thead>

    <tbody>
      <% @tweets.each do |tweet| %>
      <tr>
        <td><%= tweet.content %></td>
        <td><p><%= link_to pluralize(tweet.likes.count, 'Like' ), tweet_likes_path(tweet), method: :post %></p></td>
        <td><%= link_to '', tweets_path(tweet), :class => "fas fa-retweet" %></td>        
        <td><%= tweet.user %></td>
        <!-- <td><%= link_to 'Show', tweet %></td>
        <td><%= link_to 'Edit', edit_tweet_path(tweet) %></td>
        <td><%= link_to 'Destroy', tweet, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
